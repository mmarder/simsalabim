#pragma once
// =============================================================================
// config_secrets.h.example — template. Copy to config_secrets.h and fill in.
// config_secrets.h is git-ignored; this .example file IS committed.
// =============================================================================

#define WIFI_SSID          "YourNetwork"
#define WIFI_PASSWORD      "YourPassword"

// GitHub Personal Access Token — only for a PRIVATE repo. Empty for public.
#define GITHUB_TOKEN       ""

// Remote telemetry (optional). Both empty = disabled.
#define TELEMETRY_URL      ""   // e.g. "https://kk-status.<you>.workers.dev/ingest"
#define TELEMETRY_TOKEN    ""   // shared secret; must match the Worker's INGEST_TOKEN
